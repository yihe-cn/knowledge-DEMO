# Demo 镜像用依赖。生产仍推荐 uv + pyproject.toml。
# 与 pyproject 同步；Milvus Lite 走 pymilvus 的 extra，自带嵌入式向量库。

fastapi>=0.115
uvicorn[standard]>=0.32
sse-starlette>=2.1
pydantic>=2.9
pydantic-settings>=2.6
python-dotenv>=1.0

langgraph>=0.2.50
langchain-openai>=0.2.10
langchain-core>=0.3.20

# 向量库：嵌入式 Milvus Lite（pymilvus 内置 extra）
pymilvus[milvus_lite]>=2.4.10

# 关系库：Demo 用 SQLite（async + sync 两个驱动）；保留 aiomysql/pymysql 以便切回 MySQL
sqlalchemy[asyncio]>=2.0.36
aiosqlite>=0.20.0
aiomysql>=0.2.0
pymysql>=1.1.0
alembic>=1.13.3

# Celery 在 demo 镜像里不启动，但 import 路径要可用
celery[redis]>=5.4.0
redis>=5.0.0

# 文档解析 / 切分
python-pptx>=1.0.2
pypdf>=5.0.0
tiktoken>=0.8.0

httpx>=0.27.0
numpy>=1.26
typer>=0.12.5
python-multipart>=0.0.12
